{% extends 'base.html.twig' %}

{% block title %}Hello RegistroUsuarioController!{% endblock %}

{% block body %}
    {% for message in app.flashes('succes') %}
    <div class="flash-notice">
        {{ message }}
    </div>
    {% endfor %}

    {{ form(miFormulario) }}
{% endblock %}
